const { PermissionFlagsBits } = require('discord.js');

module.exports = {
    name: '',
    aliases: [],
    cooldown: 0,
    onlyCreator: false,
    botPermissions: [
        PermissionFlagsBits.ViewChannel,  
        PermissionFlagsBits.SendMessages
    ],
    userPermissions: [],
    execute: async (msg, args, client, color, Utils) => {

    }
}

name = el nombre del comando
aliases = los alias del comando
cooldown = el tiempo de espera ente ejecución del comando (en milisegundos)
onlyCreator = si el comando es solo para el creador del bot
botPermissions = permisos del bot necesarios para ejecutar el comando
userPermissions = permisos del usuario necesarios para ejecutar el comando
execute = codigo del comando